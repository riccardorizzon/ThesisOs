# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo

# Env & secrets
.env
.env.*
!.env.example
*-sa-key.json
service-account*.json
gcp-credentials*.json

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example

# UI reference clones (large external repos)
references/ui-libraries/
references/design-skills/

# Builder agent worktrees (orchestrate-builders skill)
.worktrees/

# Builder Memory local index (ADR-0019) — corpus cache, not source of truth
.builder-memory/

# Builder Memory local venv
builder_memory/.venv/

# Local agent config (skills under .cursor/skills/ ARE tracked)
.cursor/hooks.json
.cursor/settings.json
.impeccable/

# Local-only compose overrides (ADC mount, project wiring)
docker-compose.override.yml

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
.claude/

# Terraform plan output
tfplan
*.tfplan
